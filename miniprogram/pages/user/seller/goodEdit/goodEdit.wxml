<view>
  <view class='tab'>商品标题：<input bindblur="input" bindinput="input" data-param="good.title" maxlength='50' placeholder="好的商品标题容易吸引人"/></view>
  <view class='tab'>商品简介：<textarea bindblur="input" bindinput="input" data-param="good.inAWord" maxlength='500' placeholder="简短准确的描述可增加下单"></textarea></view>
  <view class='tab'>商品图片：
    <image src="{{good.pic}}" mode="scaleToFill"></image>
    <button catchtap="chooseGoodPic">选择图片</button>
  </view>
  <view class='tab'>价格：<input bindblur="input" bindinput="input" data-param="good.price" placeholder="免费活动可以写0" maxlength='7'/></view><!--限制数字-->
  <view class='tab'>单位：<input bindblur="input" bindinput="input" data-param="good.unit" placeholder="如：斤、个、打、位……" maxlength='2'/></view>
  <view class='tab'>商品总数：<input bindblur="input" bindinput="input" data-param="good.total"  placeholder="商品的总数" maxlength='5'/></view><!--限制数字-->
  <view class='tab'>允许在线下单：
    <radio-group class="radio-group" bindchange="input" data-param="good.lineOrder">
      <label class="radio" wx:for="{{items}}" wx:key="value">
        <radio value="{{item.value}}" checked="{{good.lineOrder == item.value}}" />
        {{item.name}}
      </label>
    </radio-group>
</view>
<view class='tab'>提供送货服务：
  <radio-group class="radio-group" bindchange="input" data-param="good.takeOut">
    <label class="radio" wx:for="{{items}}" wx:key="value">
      <radio value="{{item.value}}" checked="{{good.takeOut == item.value}}" />
      {{item.name}}
    </label>
  </radio-group>
</view>
<view class='tab'>可以预约取货/送货时间：
  <radio-group class="radio-group" bindchange="input" data-param="good.chooseTime">
    <label class="radio" wx:for="{{items}}" wx:key="value">
      <radio value="{{item.value}}" checked="{{good.chooseTime == item.value}}" />
      {{item.name}}
    </label>
  </radio-group>
</view>
<view class="dt">从
    <picker mode="selector" value="{{good.workTimeStart}}" range="{{good.arrTime}}" class="dt-item pos" bindchange="input" data-param="good.arrTimeStartIndex">
      {{good.arrTime[good.arrTimeStartIndex]}}
    </picker>
到
  <picker mode="selector" value="{{good.workTimeEnd}}" range="{{good.arrTime}}" class="dt-item pos" bindchange="input" data-param="good.arrTimeEndIndex">
      {{good.arrTime[good.arrTimeEndIndex]}}
    </picker>
</view>
<view class='tab'>有效期至：
    <picker mode="date" value="{{good.indate}}" class="dt-item pos" start="{{dateStart}}" end="{{dateEnd}}" bindchange="input" data-param="good.indate">
      {{good.indate?good.indate:'选择日期'}}
    </picker>
    <picker mode="selector" value="{{good.validTime}}" range="{{good.arrTime}}" class="dt-item pos" bindchange="input" data-param="good.validTimeIndex">
      {{good.arrTime[good.validTimeIndex]}}
    </picker>
  </view>
<view class='tab'>商品上架：
  <radio-group class="radio-group" bindchange="input" data-param="good.status">
    <label class="radio" wx:for="{{items}}" wx:key="value">
      <radio value="{{item.value}}" checked="{{good.status == item.value}}" />
      {{item.name}}
    </label>
  </radio-group>
</view>
<view class="tool">
<view class="tbtn add" catchtap="addGood">添加商品</view>
</view>
</view>